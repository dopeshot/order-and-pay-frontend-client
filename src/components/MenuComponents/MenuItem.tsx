import React from "react"

type PropTypes = {
    dishIndex: number
}
export const MenuItem: React.FunctionComponent<PropTypes> = ({ dishIndex }: PropTypes) => {


    return (
        <div>

        </div>
    )
}